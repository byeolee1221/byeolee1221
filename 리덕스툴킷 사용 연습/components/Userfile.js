import React from "react";

import classes from "./Userfile.module.css";

const Userfile = () => {
    return (
        <div className={classes.userfile_box}>
            데이터가 없습니다.
        </div>
    );
}

export default Userfile;